// console.log("hello mohit");

// const {adition, sub} = require('./math') // this is the another way we can do it

// importing built in packages
// const os = require('os')

const math = require('./math') // a type of import the modules
console.log(math.add(1,3));
// console.log(math.addition(1,3));
console.log(math.sub(4,5));
// console.log(math.subtract(1,3));

// console.log(math(3,4)); // it only work when I export it by directly exports object

